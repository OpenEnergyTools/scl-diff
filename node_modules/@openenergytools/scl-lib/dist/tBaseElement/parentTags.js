import { isSCLTag, tags } from "./tags.js";
/** @returns parent `tagName` s for SCL (2007B4) element tag  */
export function parentTags(tagName) {
    if (!isSCLTag(tagName))
        return [];
    return tags[tagName].parents;
}
//# sourceMappingURL=parentTags.js.map